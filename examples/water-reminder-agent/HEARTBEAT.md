# 💓 HEARTBEAT.md — Heartbeat Checklist

> **File purpose:** Optional tiny checklist for heartbeat runs. Keep it short to avoid token burn.

---

## Checklist

- [ ] Current time is outside quiet hours → if inside, skip and sleep until `QUIET_END`
- [ ] Select next message variant from rotation
- [ ] Send via `imsg` → log result
- [ ] Schedule next tick in `INTERVAL_MINUTES`
